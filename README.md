# 🏆 Epstein Clash Royale Database Design

> **Мозговой штурм для будущих архитекторов данных**  
> *Создай бэкенд для легендарной игры за 40 минут*

[![SQLite](https://img.shields.io/badge/SQLite-003B57?style=for-the-badge&logo=sqlite&logoColor=white)](https://sqliteonline.com)
[![GitHub](https://img.shields.io/badge/командная_работа-181717?style=for-the-badge&logo=github&logoColor=white)]()
[![Итерация 1 - Готова](https://img.shields.io/badge/Знания_-Базы_Данных-ffad33?style=for-the-badge)](##)
[![Итерация 1 - Готова](https://img.shields.io/badge/Сложность-Разработка_на_скорость-04a7cc?style=for-the-badge)](##)

<div align="center">
    <img src="docs/clideo_editor_515a55b346194e34ac964044413f171d.gif" width="500" height="300" />
</div>


---

## 🎮 Легенда

Две студии разработки — **«Красные»** и **«Синие»** — получают заказ от Supercell:  
спроектировать базу данных для игры *Clash Royale*.  

**Что должно храниться?**
- 👑 Игроки и их прогресс
- 🃏 Карты и их характеристики
- ⚔️ История сражений
- 🏰 Кланы и сообщества

---

## 🗺️ Маршрут квеста

```mermaid
graph LR
    A[Сущности] --> B[Атрибуты]
    B --> C[Типы данных]
    C --> D[Связи]
    D --> E[SQL код]
    style A fill:#ff6b6b,color:#fff
    style E fill:#4ecdc4,color:#fff
```

---

## 📦 Сущности и их атрибуты
<div align="center">
    <img src="docs/clash-royale-sweating.gif" width="300" height="300" />
</div>

### 🏰 `Clans` (Кланы)
| Поле | Тип | Описание |
|------|-----|----------|
| `id` | `INTEGER` 🏷️ | **PRIMARY KEY** — уникальный номер клана |
| `name` | `TEXT` 📛 | **NOT NULL** — название клана |
| `trophies` | `INTEGER` 🏆 | **DEFAULT 0** — общие трофеи |
| `max_members` | `INTEGER` 👥 | **DEFAULT 50** — максимум игроков |

### 👑 `Players` (Игроки)
| Поле | Тип | Описание |
|------|-----|----------|
| `id` | `INTEGER` 🏷️ | **PRIMARY KEY** — уникальный номер |
| `nickname` | `TEXT` 📛 | **NOT NULL** — имя игрока |
| `level` | `INTEGER` ⬆️ | **CHECK (1-14)** — уровень башни |
| `experience` | `INTEGER` ✨ | **DEFAULT 0** — опыт |
| `arena` | `TEXT` 🏟️ | Название арены |
| `clan_id` | `INTEGER` 🔗 | **FOREIGN KEY** → `Clans.id` |

### 🃏 `Cards` (Карты)
| Поле | Тип | Описание |
|------|-----|----------|
| `id` | `INTEGER` 🏷️ | **PRIMARY KEY** |
| `name` | `TEXT` 📛 | **UNIQUE NOT NULL** — название |
| `elixir_cost` | `INTEGER` ⚡ | Стоимость эликсира |
| `rarity` | `TEXT` 💎 | Common / Rare / Epic / Champion |

### ⚔️ `Battles` (Сражения)
| Поле | Тип | Описание |
|------|-----|----------|
| `id` | `INTEGER` 🏷️ | **PRIMARY KEY** |
| `winner_id` | `INTEGER` 🥇 | **FOREIGN KEY** → `Players.id` |
| `loser_id` | `INTEGER` 😢 | **FOREIGN KEY** → `Players.id` |
| `battle_date` | `DATETIME` 📅 | **DEFAULT CURRENT_TIMESTAMP** |
| `duration_seconds` | `INTEGER` ⏱️ | Длительность боя |

---

## 🔌 Шпаргалка по SQLite

### Создание таблицы
```sql
CREATE TABLE IF NOT EXISTS Players (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    nickname TEXT NOT NULL,
    level INTEGER CHECK(level BETWEEN 1 AND 14),
    clan_id INTEGER,
    FOREIGN KEY (clan_id) REFERENCES Clans(id) ON DELETE SET NULL
);
```
<div align="center">
    <img src="docs/clash-royale-goblinstein.gif" width="300" height="300" />
</div>

### Вставка данных
```sql
INSERT INTO Clans (name, trophies) 
VALUES ('Русские Викинги', 15000);

INSERT INTO Players (nickname, level, arena, clan_id) 
VALUES ('Король_Вова', 12, 'Legendary Arena', 1);
```

### Полезные типы данных
| Тип | Для чего | Пример |
|-----|----------|--------|
| `INTEGER` | Числа, ID, уровни | `level 14` |
| `TEXT` | Имена, названия | `'Хог Райдер'` |
| `DATETIME` | Дата и время | `2024-01-20 15:30:00` |
| `BOOLEAN` | Да/Нет (0 или 1) | `is_online 1` |

### Ключевые слова-помощники
```sql
NOT NULL            -- поле обязательно
DEFAULT 0           -- значение по умолчанию
UNIQUE              -- только уникальные значения
CHECK (level > 0)   -- проверка условий
PRIMARY KEY         -- главный ключ таблицы
FOREIGN KEY         -- ссылка на другую таблицу
```

---

## 🎯 Чек-лист для команд

- [ ] Обсудили сущности (о чём будем хранить данные?)
- [ ] Определили атрибуты (какие факты важны?)
- [ ] Выбрали типы данных (TEXT или INTEGER?)
- [ ] Продумали связи (FOREIGN KEY)
- [ ] Написали CREATE TABLE
- [ ] Добавили тестовые данные (INSERT)
- [ ] Проверили в SQLite Online

<div align="center">
    <img src="docs/clash-royale-clash.gif" width="300" height="300" />
</div>

---

## 💡 Вопросы для размышления

<div align="center">
    <img src="docs/clash-royale-clash-of-clans (1).gif" width="300" height="300" />
</div>

<details>
<summary>🤔 Как хранить колоду игрока?</summary>

В реальной игре нужна отдельная таблица `Decks`:
- `id` — номер колоды
- `player_id` — владелец
- `card_id` — карта в колоде
- `slot` — позиция (1-8)
</details>

<details>
<summary>⚡ Что если ничья в битве?</summary>

Можно добавить поле `battle_result`:
- `winner_id` + `loser_id` (победа)
- `NULL` + `NULL` + `result = 'draw'` (ничья)
</details>

<details>
<summary>📊 Как хранить уровень карт игрока?</summary>

Нужна связующая таблица `PlayerCards`:
- `player_id`
- `card_id`  
- `level` (уровень карты)
- `count` (количество копий)
</details>
---

## ⚡ Быстрый старт в SQLite Online

1. Открой [sqliteonline.com](https://sqliteonline.com)
2. Выбери **File → New Database**
3. Копируй SQL-код слева
4. Нажимай **Run** (▶️)
5. Смотри результат в таблицах справа

```sql
-- Быстрый тест для проверки
CREATE TABLE test (message TEXT);
INSERT INTO test VALUES ('🔥 База готова к бою!');
SELECT * FROM test;
```

---

## 🏆 Финальный бонус

<div align="center">
    <img src="docs/clash-royale-emote.gif" width="300" height="300" />
</div>


**Помни:** хорошая база данных как легендарная карта —  
должна быть сбалансированной, продуманной и надежной!

```sql
-- Напутствие ментора
SELECT '🚀 У вас всё получится! 🚀' AS motivation;
```

> [!NOTE]
> Важно понять какие у вас задачи и разделить разработку на итерации - это экономит время для общей задачи.

<br>

> [!TIP]
> Разделяйтесь по ролям, больше всего пользы можно принести там где у вас больший опыт и к чему есть желание.

<br>

> [!IMPORTANT]
> Разделяйте зону инструментов и компетенций, одни могут следить за общим темпом и отлаживать прогресс, другие могут готовить материаллы для будущей итерации.
> <br>
> Коммиты считаются вкладом разработчиков в проект, документация и ее представление - это основной вклад команды.

<br>

> [!WARNING]
> Представленные итоговые таблицы должны быть в этом же репозитории в ветке команды с коммитами участников команды, скриншоты из SQLITE ONLINE так же должны быть там.
> <br>
> Лучшим представлением может быть хорошо и аккуратно составленный файл документации на языке разметки `markdown` со скриншотами команд, результатом вывода данных и рукописными таблицами.
> <br>
> Если в ветке команды будут Issues или диаграммы хода разработки - это будет дополнительным баллом!

<br>

> [!CAUTION]
> Ваше участие в командной разработке должно быть подтверждено старшим разработчиком команды или ментором, для этого указываются роли всех разработчиков и их вклад.
> <br>
> Итоговый высщий счет и победу одержит команда которая по завершению времени предоставит наилучший реализованный результат.
---
<div align="center">
    <img src="docs/clash-royale-slow-clap.gif" width="300" height="300" />
</div>


<div align="center">

**Создано для будущих дата-инженеров Top College**  
*#SQL #ClashRoyale #TeamWork #DataDesign*

</div>
